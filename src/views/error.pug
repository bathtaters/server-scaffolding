extends base

block content
  .container
    h2= header
    if error.status
      h5: i.small &nbsp;Error code: #{error.status}
    p= error.message
    if showStack
      pre= error.stack
    a.button(href="..") Back
