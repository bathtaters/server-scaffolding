extends base

block scripts
  script(src="/javascripts/togglePassword.js")

block content
  .row

    .card.large.col.center
      form(action=postURL method="POST")
        fieldset
          legend= isUser ? "Login" : "Create Admin"

          .input-group.fluid
            label(for="username") Username
            input#username(
              type="text" name="username" placeholder="Enter Username" autocomplete="username"
              required=true minLength=limits.username.min maxLength=limits.username.max
            )

          .input-group.fluid
            label(for="password") Password
            input#password(
              type="password" name="password" placeholder="Enter Password" 
              autocomplete=(isUser ? "current-password" : "new-password")
              required=true minLength=limits.password.min maxLength=limits.password.max
            )
            button.toggle-password.small(type="button")

          .button-group
            input.button(type="submit" value=(isUser ? "Login" : "Create"))
          
          if failureMessage.length
            .toast= failureMessage.join(', ')