extends ../../../src/views/components/base.pug

block head
  link(rel='stylesheet', href='/stylesheets/engine.css')
  block header

block body
  #siteContainer
    include ../../../src/views/components/header.pug
    
    #pageContainer
      block content
    
    include footer.pug
    
    script(
      src=urls.jquery.src,
      integrity=urls.jquery.integrity,
      crossorigin="anonymous"
    )
    input#_formKeys.hidden(type="hidden" value=JSON.stringify(formKeys))
    script(src="/javascripts/common.client.js")
    block scripts