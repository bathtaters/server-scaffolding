export const nodeEnv = ['development','secure-dev','production','test'] as const

export const settingsActions = {
    update:   'Update',
    default:  'Default',
    undo:     'Undo',
    restart:  'Restart',
} as const